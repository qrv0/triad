# Archives

This directory preserves the repository state before the clean rebuild.

- Snapshot: `pre-clean-2026-05-20_09-45-55`
- Repository: `triad`
- Purpose: keep the full public/history ledger intact while rebuilding the root cleanly.
- Note: `.git/` was not moved, so commit history remains attached to the repository.

The snapshot contains all tracked and untracked working-tree files that were present at archive time, excluding only `.git/` and this `archives/` container.
